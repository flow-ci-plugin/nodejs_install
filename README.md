
# nodejs_install Step
Install dependencies use `npm install`

### INPUTS

## EXAMPLE 

```yml
steps:
  - name: nodejs_install
    enable: true
    failure: true
    plugin:
      name: nodejs_install
      inputs:
```
